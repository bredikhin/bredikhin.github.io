---
export interface Props {
  name: string;
  img: string;
  url: string;
}

const { name, img, url } = Astro.props;
---

<a href={url} target="_blank" rel="noreferrer noopener nofollow">
  <img src={`/images/${img}`} alt={name} class="logo" />
</a>
